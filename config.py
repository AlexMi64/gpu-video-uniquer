# Конфигурационный файл для уникализатора видео
# Все настройки здесь можно изменять для кастомизации обработки

config = {
    # Сдвиг оттенка (hue) в градусах - легкое изменение цвета, 5-10 для незаметности
    'hue_shift': 5,

    # Изменение насыщенности цветов, 0.05 добавит немного яркости
    'saturation_shift': 0.05,

    # Текст водяного знака в правом нижнем углу
    'watermark_text': "UNIQUE",

    # Размер шрифта водяного знака
    'fontsize': 24,

    # Цвет водяного знака в RGB (красный, зеленый, синий)
    'watermark_color': [255, 255, 255],  # Белый

    # Горизонтальная позиция водяного знака (left, center, right)
    'watermark_hpos': 'right',

    # Вертикальная позиция водяного знака (top, middle, bottom)
    'watermark_vpos': 'bottom',

    # Смещение видео по X (горизонталь) в пикселях - обрежет левую часть
    'shift_offset_x': 10,

    # Смещение видео по Y (вертикаль) в пикселях - обрежет верхнюю часть
    'shift_offset_y': 5,

    # Плотность случайных точек на кадре - уменьшено для скорости
    'dots_density': 0.0001,

    # Минимальное значение цвета точек (0-255) - начнем от серого
    'dots_color_min': 100,

    # Максимальное значение цвета точек - до светло-серого, чтобы сливались
    'dots_color_max': 200,

    # Вероятность визуальных помех (инверсия областей) на кадре - уменьшено для скорости
    'glitch_density': 0.005,

    # Частота шумов на кадрах - каждый N-й кадр будет с шумом - увеличено для скорости
    'noise_frequency': 200,

    # Директория для поиска видео файлов (точки означает текущую папку)
    'process_directory': ".",

    # Поворот видео по часовой стрелке в градусах - легкий наклон для уникальности
    'rotation_degrees': 1.5,

    # Искать видео в подпапках рекурсивно (true) или только в корне (false)
    'recursive': False,
}
